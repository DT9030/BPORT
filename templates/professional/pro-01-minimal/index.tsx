"use client"

// Example connector that re-exports the built-in Pro-01 so it's also available via templates/ path
import { ProfessionalMinimalTemplate } from '@/components/templates/professional-templates'
export default ProfessionalMinimalTemplate
